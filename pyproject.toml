[build-system]
requires = [
    "setuptools>=69",
    "setuptools_scm[toml]>=8",
    "packaging>=24.2",
    "wheel",
    "cmake>=3.18",
    "nanobind>=2.4",
]
build-backend = "setuptools.build_meta"

[project]
name = "cheby"
dynamic = ["version"]
description = "Functions represented as Chebyshev series"
readme = { file = "README.md", content-type = "text/markdown" }
license = "MIT"
requires-python = ">=3.9"
authors = [
    { name = "Gwénaël Gabard", email = "gwenael.gabard@univ-lemans.fr" },
]
dependencies = [
    "numpy>=1.20",
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: C++",
    "Operating System :: OS Independent",
    "Topic :: Scientific/Engineering :: Mathematics",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0",
    "sympy>=1.12",
]

[project.urls]
Homepage = "https://github.com/GwenaelGabard/cheby"
Repository = "https://github.com/GwenaelGabard/cheby"
Issues = "https://github.com/GwenaelGabard/cheby/issues"

[tool.cibuildwheel]
build = ["cp39-*", "cp310-*", "cp311-*", "cp312-*", "cp313-*"]
skip = ["*-win32", "*-manylinux_i686"]
dependency-versions = "latest"

[tool.cibuildwheel.macos]
environment = { MACOSX_DEPLOYMENT_TARGET = "11.0" }

[tool.setuptools_scm]
fallback_version = "0.0.0-dev"

[tool.ruff]
line-length = 88
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]

[tool.pytest.ini_options]
testpaths = ["tests"]
